# AAC 社群知识库安全指引（AGENT 可执行版）

## 1. 风险范围
- 站点类型：纯静态 HTML/CSS/JS（GitHub Pages 托管）。
- 重点防护：外链、脚本注入、文件完整性、仓库权限和误发布。

## 2. 发布前强制检查
- 外链安全：新增或修改 `http://`、`target="_blank"` 链接时必须可追溯和审阅。
- 脚本安全：禁止新增 `eval`、`Function`、未过滤字符串拼接执行脚本。
- 输入安全：搜索/跳转逻辑不得用 `innerHTML` 直接拼接不可信内容。
- 文件完整性：页面引用的 CSS/JS/图片路径全部存在。
- 数据安全：不得把 token、密钥、账号、敏感隐私文件写入仓库前端。

## 3. 事后发现异常的处理
- 第一优先级：停止继续发布（或不再点击“可见”状态）。
- 回滚：`git revert` 可疑提交并推回 main，待复核。
- 定位：恢复版本后逐项对比提交内容，找到风险入口。
- 修复：更新 `AGENTS.md` 的安全红线/协作规则后再继续发布。

## 4. 协作约束（所有维护者）
- main 仅允许 PR 合并，不允许直接推送。
- 每次 PR 说明修改范围和影响页面。
- 每次上线前至少一名维护者确认安全红线。
