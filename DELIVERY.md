# 交付与继续开发说明

## 当前版本

- 源分支：`codex/ai-knowledge-base-redesign`
- 最新提交：本次 Codex × Claude Code 整合提交（以 `git log` 为准）
- 当前工作树：整合完成后应保持 clean
- 交付目录：`/Users/rita/Downloads/知识库/`
- 本次备份：`/Users/rita/Downloads/知识库/backups/knowledge-base-backup-20260829-131506`
- 版本记录：`backups/VERSIONS.md`

## 2026-08-30 整合说明

- Claude Code 的 AI 新手入门第 1 章、六章顺序、6 个演示组件、学习页样式、章节索引和对应浏览器/运行时测试已合入。
- Codex 的回放封面、Copilot 精选课程迁移到 AI 工具与资源、小 A 能力面板精简和资源/视频 QA 断言已保留。
- 整合规则见 [`docs/agent-collaboration.md`](docs/agent-collaboration.md)。后续不要用交接包整目录覆盖站点；按改动清单和基线 SHA 合并。

## 已完成能力

### 首页

- 三个主要入口：AI 新手入门、录播回放、AI 工具与资源。
- 每张卡使用大号蓝色图标，标题/说明/动作层级清晰。
- 小 A 保留为辅助快捷入口，文案为“打开小 A ↗”，指向 Portal 新标签页。
- 首页 Hero 与入口文案已精简，人物左移并避让标题和按钮。

### AI 新手入门

- 学习目录页 + 六章详情页 + 「小白三千问」FAQ（32 条，四组）。
- 桌面左侧 sticky 学习路径栏，当前章展开小节清单并逐条打勾；窄屏使用原生可展开目录。
- 每章右侧显示「已读小节 / 总小节」比例，与目录页卡片同口径。
- 六章包含案例、轻互动、快速自测、可复制模板和下一章/返回路径。
- 不设分数、考试、证书、注册或综合实战。
- 第 1 章的关系图使用“基础能力轨道 + Agent 外接机制”表达，避免误读为线性包含。

### 首次进入与重复进入

两套视图，都只依赖 `sessionStorage`：

| | 首次（本次浏览没动过任何一章） | 回访（至少动过一章） |
| --- | --- | --- |
| 目录页 | CTA「从第 1 章开始」，第一张卡带「建议从这里开始」 | CTA「接着读 · 第 N 章 …」，显示「已看 x / 6」和「上次读到「章 · 小节」」 |
| 详情页 | 从顶部开始 | 顶部横幅「上次读到…」+ [跳到那里] / [从头读起]；已看过的章给 [直接看要点] |

关键口径：**打开页面 ≠ 正在看**。章节状态要读满 2 个小节才升为 `in-progress`，由 `markSectionRead` 通过 IntersectionObserver 触发。改版前 `markStarted` 在页面加载瞬间触发，导致目录页六张卡同时显示「正在看」却又写「已看 0 / 6」，两个口径互相打架。

目录页静态 HTML 里的「已看 0 / 6」是无 JS 时的兜底文案，被 `test-learning-experience.mjs` 钉死，不要改；首次/回访的切换只在运行时发生。

### 资源与其他页面

- AI 工具与资源承接外部工具、站点、课程、视频、博主、论文等。
- 录播页和资源页复用现有小 A 视觉资产，但不把外部内容塞回新手学习页。
- 全站页脚统一为：
  `由 Amersports AI Community 维护的内部学习平台，帮助每一位员工从 AI 新手成长为高效使用者。`

## 内容规范

2026-08-29 改版：六章按三块组织（AI 是什么 / 怎么和 AI 沟通 / 怎么用 AI），完整目录见 `README.md`。章节 id 未变，只改了数组顺序、编号和标题，旧书签与别名映射照常可用。

覆盖 20 个基础概念，内容以内部 Obsidian 沉淀（`~/瑞塔的知识库`）为基础向非技术职场用户降维改写。学习路径的结构装置参考了小山学堂，但文案、例子和互动均为本项目重写：

- 信任四象限（有没有标准答案 × 错了代价多大）
- 「说不清要什么？让它来问你」
- 每小节配一个生活化类比

**新写的三块**（Obsidian 库里零沉淀，不要当成有出处的内容去核）：反模式清单、让它复述确认、数据红线。其中数据红线只写通用原则并指向公司 IT 与合规政策，**不得写入具体内部规定、可用工具清单或价格**。

## 修改流程

1. 先阅读 `docs/specs/` 中相关设计规范，再阅读对应 `docs/plans/`。
2. 修改源目录 `site/knowledge-base/`，不要直接改 Downloads 交付副本。
3. 运行静态、运行时、浏览器契约；至少确认无渐变、无横向溢出、链接和旧别名未回归。
4. 在 Downloads 下创建新的时间戳备份；不得删除、覆盖旧备份。
5. 同步时排除顶层 `backups/`，同步后执行：

   ```bash
   rsync -aicn --delete --exclude backups/ site/knowledge-base/ /Users/rita/Downloads/知识库/
   ```

   输出应为空。

6. 交付前确认 `git status --short` 为空，并在浏览器打开交付首页。

## 已知兼容性

- 旧学习 URL 通过别名映射到新章节；公司/模型旧目录显示迁移提示并指向资源页。
- 文件协议下部分浏览器能力受安全策略限制，浏览器 QA 使用本地只读服务；不应因此改回外部依赖。
- 交付目录的 `backups/` 是历史资料，不属于现行站点扫描范围；扫描器只跳过交付根的直接 `backups` 子目录。

## 参考

学习路径的展示方式参考了小山学堂的章节路径与轻量推进节奏，但内容、文案和互动均为本项目重写：
https://xueai.app/slides/learn.html#zero-0.html
